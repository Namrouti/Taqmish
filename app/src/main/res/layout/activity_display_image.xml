<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Activities.DisplayImageActivity">

    <!-- ImageView for the selected image -->
    <ImageView
        android:id="@+id/selectedImageView"
        android:layout_width="0dp"
        android:layout_height="350dp"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:contentDescription="Selected Image" />
    <!-- NEW result image (from OpenAI) -->
    <ImageView
        android:id="@+id/editedImageView"
        android:layout_width="0dp"
        android:layout_height="250dp"
        android:layout_marginTop="8dp"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toBottomOf="@id/selectedImageView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:contentDescription="Edited Result" />

    <!-- RecyclerView for displaying similar images -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/similarImagesRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/editedImageView"
        app:layout_constraintBottom_toBottomOf="parent"
        android:padding="10dp"
        android:orientation="horizontal"
        tools:listitem="@layout/item_similar_image" />

</androidx.constraintlayout.widget.ConstraintLayout>
